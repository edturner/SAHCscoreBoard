# name: Update Scoreboard Data

# on:
#   workflow_dispatch:
#     inputs:
#       start:
#         description: "Start date dd/mm/YYYY (optional)"
#         required: false
#         default: ""
#       end:
#         description: "End date dd/mm/YYYY (optional)"
#         required: false
#         default: ""
#   schedule:
#     - cron: '0 3 * * 4,5'     # Thu/Fri 03:00 UTC initial build
#     - cron: '*/5 * * * 6,0'   # Every 5 min Sat/Sun UTC (Live Scores)

# jobs:
#   update-data:
#     runs-on: ubuntu-latest
#     concurrency:
#       group: scoreboard-data
#       cancel-in-progress: true
#     permissions:
#       contents: write
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.x'

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt || pip install requests beautifulsoup4 pytz

#       - name: Update Scoreboard Data
#         run: |
#           if [ -n "${{ github.event.inputs.start }}" ]; then
#             python scripts/gms_fetcher.py update-scoreboard --weekend "${{ github.event.inputs.start }}"
#           else
#             python scripts/gms_fetcher.py update-scoreboard
#           fi

#       - name: Commit and push changes
#         run: |
#           git config user.name "github-actions"
#           git config user.email "github-actions@users.noreply.github.com"
#           git add data/scoreboard/weekend_fixtures.json data/scoreboard/mens_fixtures.csv data/scoreboard/womens_fixtures.csv || true
#           git commit -m "Update scoreboard data [skip ci]" || echo "No changes"
#           git push

